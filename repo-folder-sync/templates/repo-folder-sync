#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Friendly argv[0] so process/background UI shows repo-folder-sync.
exec -a repo-folder-sync /bin/bash "$SCRIPT_DIR/watch.sh"
